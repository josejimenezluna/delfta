name: delfta5
channels:
  - defaults
  - conda-forge

dependencies:
  - python
  - openbabel
  - xtb=6.3.1 # pin for delfta compatibility
  - h5py
  - numpy
  - pandas
  - scikit-learn
  - scipy
  - matplotlib
  - tqdm
  - joblib
  - einops
  - pip
  - pytest
  - pip:
      - ase
      - networkx
      - torch --index-url https://download.pytorch.org/whl/cpu # install wheels for your required architecture
      - torch_geometric
